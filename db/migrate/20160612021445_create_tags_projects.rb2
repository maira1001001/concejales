class CreateTagsProjects < ActiveRecord::Migration
  def change
    create_table :tags_projects, id: false do |t|
      t.belongs_to :tags, index: true
      t.belongs_to :projects, index: true
    end
  end
end
