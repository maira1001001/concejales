<div class="page-header">
  <div class="row">
    <div class="col-md-7">
      <h1><%= t '.title'  %></h1>
    </div>
    <div class="col-md-5">
      <div class="header-actions">
        <%= link_to new_councilor_path,
          class: 'btn btn-primary' do %>
          <%= icon 'plus'  %>
          <%= t "councilors.actions.new" %>
          <% end %>
      </div>
    </div>
  </div>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
                <th><%= t 'global.index.actions' %></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @councilors) do |councilor| %>
                <td>
          <%= link_to icon('eye-open'), councilor,
            class: 'btn btn-xs btn-default',
            title: t('councilors.actions.show', default: t('global.actions.show')),
            data: { toggle: 'tooltip', container: 'body', placement: 'left' } %>
          <%= link_to icon('pencil'), edit_councilor_path(councilor),
            class: 'btn btn-xs btn-primary',
            title: t('councilors.actions.edit', default: t('global.actions.edit')),
            data: { toggle: 'tooltip', container: 'body', placement: 'left' } %>
          <%= link_to icon('trash'), councilor,  method: :delete,
            class: 'btn btn-xs btn-danger',
            title: t('councilors.actions.destroy', default: t('global.actions.destroy')),
            data: { confirm: t('councilors.actions.confirm_destroy', default: t('global.actions.confirm_destroy')), toggle: 'tooltip', container: 'body', placement: 'left' } %>
      </td>
      <% end %>
    </tbody>

  </table>
</div>
