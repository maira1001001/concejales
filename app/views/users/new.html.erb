<%= render partial: 'page_header', locals: { title: t('users.new.title'), sub_title: 'Ingrese los datos que se solicitan para invitar al usuario al sistema. Luego, el usuario debe recibir un email y confirmar su participación en el sistema.' } %>

<%= render 'form' %>
